namespace Peak.Player.States
{
    /// <summary>기절자 업기, 속도 −30%, Climbing 진입 불가 (202 3장, T1). 자리만 — 구현은 M3.</summary>
    public sealed class CarryingState : PlayerState
    {
        public CarryingState(PlayerController controller) : base(controller)
        {
        }

        // M1
    }
}
