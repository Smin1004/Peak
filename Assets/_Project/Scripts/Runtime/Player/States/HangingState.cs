namespace Peak.Player.States
{
    /// <summary>로프·피톤 잡음 (202 3장, 206). 자리만 — 구현은 M1 이후 (206 로프·피톤).</summary>
    public sealed class HangingState : PlayerState
    {
        public HangingState(PlayerController controller) : base(controller)
        {
        }

        // M1
    }
}
