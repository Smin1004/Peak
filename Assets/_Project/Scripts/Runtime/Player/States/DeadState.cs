namespace Peak.Player.States
{
    /// <summary>기절 타임아웃·즉사 구역, 유령 카메라 (202 3장). 자리만 — 구현은 M3.</summary>
    public sealed class DeadState : PlayerState
    {
        public DeadState(PlayerController controller) : base(controller)
        {
        }

        // M1
    }
}
